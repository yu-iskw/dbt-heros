# Semantic Layer Rules

1.  **Trust Semantic Layer**: Trust `get_explore` and `compile_query` as the definitive source of truth for metrics and dimensions.
2.  **Verify Logic**: Use `compile_query` to verify the logic of a metric before including it in an analysis report.
